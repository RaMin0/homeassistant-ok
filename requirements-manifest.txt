# Mirror of custom_components/ok/manifest.json requirements for Dependabot and pip-audit.
# Keep these entries in the same order as the manifest requirements; tests enforce this.
google-auth==2.54.0
google-cloud-firestore==2.27.0
